########################################
# DockGo Environment Configuration
########################################

########################
# Server Configuration
########################

# Port DockGo listens on
PORT=3131

# Allowed CORS origin.
# Change this in production to your frontend origin.
# Avoid using '*' unless you fully understand the security implications.
CORS_ORIGIN=http://localhost:3131


########################
# Authentication
########################

# --- Option 1: Legacy API Token (Less Recommended) ---
# Must be long, random, and unguessable (32+ characters).
# Example generation:
#   openssl rand -hex 32
#
# API_TOKEN=replace_with_strong_random_token


# --- Option 2: User Login (Recommended) ---
# Enables username/password authentication with signed sessions.
#
# AUTH_USERNAME=admin
# AUTH_PASSWORD=replace_with_strong_password
#
# Required for signing session cookies.
# Must be a long random secret.
# Example generation:
#   openssl rand -hex 32
#
# AUTH_SECRET=replace_with_random_secret
#
# Optional: bcrypt hashing cost (default: 10, min: 4, max: 31)
# Higher = more secure but more CPU usage.
# AUTH_BCRYPT_COST=10


########################
# Compose Security
########################

# SECURITY FEATURE:
# Restrict Compose working directories to specific base paths.
# Strongly recommended if DockGo is exposed beyond localhost.
#
# Comma-separated list of allowed base paths.
# If empty, any valid directory is allowed.
#
# Example:
# ALLOWED_COMPOSE_PATHS=/opt/docker,/home/user/docker,/data/compose
#
# ALLOWED_COMPOSE_PATHS=


########################
# Notifications (Apprise)
########################

# One or multiple providers separated by comma.
#
# Examples:
# APPRISE_URL=gotify://192.168.1.204/Aon5xAnYo-hTn70
# APPRISE_URL=ntfy://tk_token@192.168.1.161:3500/topic
#
# Optional: Max queued notifications before dropping new ones (default: 200)
# APPRISE_QUEUE_SIZE=200


########################
# Background Scan Schedule
########################

# How often DockGo checks for container updates.
# Valid units:
#   s = seconds
#   m = minutes
#   h = hours
#
# Examples:
#   12h  (12 hours)
#   30m  (30 minutes)
#
# Avoid very short intervals (<5m) to prevent
# Docker Hub or registry rate limiting.
#
SCAN_INTERVAL=24h


########################
# Logging
########################

# Log format:
#   json  -> structured logs (recommended for Docker/production)
#   text  -> human-readable logs
#
# Default:
#   JSON when running inside Docker
#   Text otherwise
#
# LOG_FORMAT=json

# Log level:
#   debug, info, warn, error
#
# LOG_LEVEL=info